"""Vendored agent runtimes that ship with ax-cli.

Subpackages here are runtime adapters that the gateway can launch directly,
without an external clone or environment variable hunt.

Today: `hermes` (Hermes Agent sentinel) — see `ax_cli.runtimes.hermes`.
"""
